// fs模块就是 对文件和目录进行操作的， 这个模块可以完成linux指令对目录和文件能做的一切操作

// 同步操作的方法写上sync， 异步操作的方法默认什么也不带

/**
 * 常用 得方法有fs.mkdir 创建目录，fs.rmdir 删除目录， fs.writeFile 异步写文件， fs.readFile 异步读文件，
 * fs.unlink 异步删除文件, fs.createReadStream(path) 获取读取文件的stream， fs.createWriteStream
 * 获取写入文件的stream
 *
 */